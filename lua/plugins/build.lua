return {
   "castlele/build.nvim",
   opts = true,
   -- TODO: Update build.nvim with mini.picker or just deprecate the plugin itself
   dependencies = {
      "nvim-telescope/telescope.nvim",
   },
}
